# node.ts-canil-moonteh

Projeto feito no módulo node + typescript da plataforma b7web

### Pré-requisitos globais:
`npm i -g nodemon typescript ts-node`

### Para rodar o projeto:
`npm run start-dev`